# Uncomment this line to define a global platform for your project
# platform :ios, '9.0'

target 'TwitterAuth' do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'Fabric'
  pod 'TwitterKit'

  # Pods for TwitterAuth

  target 'TwitterAuthTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'TwitterAuthUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
